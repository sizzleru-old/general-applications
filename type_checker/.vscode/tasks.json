{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Generate code coverage",
            "type": "shell",
            "command": "coverage run --branch -m type_checker.tests.__init__; coverage report -m; coverage html -d coverage_reports",
            "problemMatcher": [],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}